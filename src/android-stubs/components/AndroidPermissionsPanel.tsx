// Android 权限面板组件桩 - macOS 构建（渲染空节点）
import React from 'react';

interface AndroidPermissionsPanelProps {
  mode: string;
}

export const AndroidPermissionsPanel: React.FC<AndroidPermissionsPanelProps> = () => {
  return null;
};

export default AndroidPermissionsPanel;
